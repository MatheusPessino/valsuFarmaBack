package com.api.valsuFarmaBack.repositories;

import com.api.valsuFarmaBack.domain.LogUpdateBrand;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogUpdateBrandRepository extends JpaRepository<LogUpdateBrand, Integer> {
}
